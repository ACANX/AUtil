<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.acanx.util</groupId>
        <artifactId>autil-json</artifactId>
        <version>${revision}</version>
        <relativePath>../pom.xml</relativePath> <!-- 指定父POM路径 -->
    </parent>

    <groupId>com.acanx.util</groupId>
    <artifactId>json-jackson</artifactId>
    <version>${revision}</version>
    <packaging>jar</packaging>
    <name>JSON-Jackson</name>
    <description>JSON-Jackson 工具库</description>
    <url>https://github.com/ACANX/AUtil</url>

    <modules>

    </modules>


    <dependencies>
        <dependency>
            <groupId>com.acanx.util</groupId>
            <artifactId>autil-core</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>com.acanx.util</groupId>
            <artifactId>json-core</artifactId>
            <version>${revision}</version>
        </dependency>
        <!-- 契约测试基类（test-jar） -->
        <dependency>
            <groupId>com.acanx.util</groupId>
            <artifactId>json-core</artifactId>
            <version>${revision}</version>
            <type>test-jar</type>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>${jackson.version}</version>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.datatype</groupId>
            <artifactId>jackson-datatype-jsr310</artifactId>
            <version>${jackson.version}</version>
        </dependency>
        <!-- Jackson 3：迁移期新增，optional 保证默认不传递给下游；
             jsr310 已合入 databind（tools.jackson.databind.ext.javatime），无需单独依赖；
             注解仍为 com.fasterxml.jackson.annotation（2.x），与 Jackson 2 共用同一 artifact，无类冲突 -->
        <dependency>
            <groupId>tools.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>${jackson3.version}</version>
            <optional>true</optional>
        </dependency>

        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <version>${slf4j-api.version}</version>
            <scope>compile</scope>
        </dependency>

        <dependency>
            <groupId>com.acanx.meta.model</groupId>
            <artifactId>model-test</artifactId>
            <version>${meta-open.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <version>${junit-jupiter.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <!-- 本模块不依赖注解处理器，禁用自动发现，避免加载 CopierAstProcessor 等处理器导致 JDK 21 编译失败 -->
                    <proc>none</proc>
                </configuration>
            </plugin>
            <!-- 生成Javadoc，关闭doclint,避免注解检查不通过 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <version>${maven-javadoc-plugin.version}</version>
                <executions>
                    <execution>
                        <id>attach-javadocs</id>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                        <configuration>
                            <!-- <additionalparam>-Xdoclint:none</additionalparam>-->
                            <!-- <aggregate>true</aggregate>-->
                            <charset>${project.build.sourceEncoding}</charset> <!-- utf-8读取文件 -->
                            <encoding>${project.build.sourceEncoding}</encoding> <!-- utf-8进行编码代码 -->
                            <docencoding>${project.build.sourceEncoding}</docencoding> <!-- utf-8进行编码文档 -->
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
